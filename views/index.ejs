<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DevInsight – GitHub Repo Analyzer</title>
  <style>
    * { box-sizing: border-box; }
    body { font-family: 'Segoe UI', system-ui, sans-serif; margin: 0; min-height: 100vh; background: linear-gradient(135deg, #0f0f1a 0%, #1a1a2e 50%, #16213e 100%); color: #e4e4e7; display: flex; align-items: center; justify-content: center; padding: 1rem; }
    .card { background: rgba(30, 30, 46, 0.9); border-radius: 12px; padding: 2rem; max-width: 520px; width: 100%; box-shadow: 0 8px 32px rgba(0,0,0,0.4); border: 1px solid rgba(255,255,255,0.06); }
    h1 { margin: 0 0 0.5rem; font-size: 1.75rem; font-weight: 600; }
    .sub { color: #71717a; font-size: 0.9rem; margin-bottom: 1.5rem; }
    .error { background: rgba(239, 68, 68, 0.2); border: 1px solid rgba(239, 68, 68, 0.4); color: #fca5a5; padding: 0.75rem 1rem; border-radius: 8px; margin-bottom: 1rem; font-size: 0.9rem; }
    form { display: flex; flex-direction: column; gap: 1rem; }
    input[type="text"] { width: 100%; padding: 0.85rem 1rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.12); background: rgba(0,0,0,0.25); color: #fff; font-size: 1rem; }
    input[type="text"]::placeholder { color: #71717a; }
    input[type="text"]:focus { outline: none; border-color: #6366f1; }
    button { padding: 0.85rem 1.5rem; border-radius: 8px; border: none; background: linear-gradient(90deg, #6366f1, #8b5cf6); color: #fff; font-size: 1rem; font-weight: 600; cursor: pointer; }
    button:hover { opacity: 0.95; transform: translateY(-1px); }
    button:disabled { opacity: 0.6; cursor: not-allowed; transform: none; }
  </style>
</head>
<body>
  <div class="card">
    <h1>DevInsight</h1>
    <p class="sub">Analyze any GitHub repository: structure, routes, controllers, models, and execution flow.</p>
    <% if (typeof error !== 'undefined' && error) { %>
      <div class="error"><%= error %></div>
    <% } %>
    <form action="/analyze" method="POST">
      <input type="text" name="repoUrl" placeholder="https://github.com/username/repo" required value="<%= typeof repoUrl !== 'undefined' ? (repoUrl || '') : '' %>" />
      <button type="submit">Analyze repository</button>
    </form>
  </div>
</body>
</html>
